PORT=3000
DB_PATH=./data/app.db

ADMIN_KEY= #Admin key for creating invites, should be kept secret   

USER_TTL_DAYS=14 #User expiration time in days
INVITE_TTL_HOURS=24 #Invite expiration time in hours
SESSION_TTL_MINUTES=60 #Session expiration time in minutes
COOKIE_NAME=sid #Cookie name for session ID
TTL_ENABLED=true #Enable TTL cleanup
TTL_CRON=0 3 * * * #Cron schedule for TTL cleanup
INVITE_USED_RETENTION_HOURS=24 #Retention time for used invites in hours

# WebAuthn settings
RP_ID=localhost
RP_NAME=Backend demo
ORIGIN=http://localhost:3000

#
AI_MODE=stub
AI_BASE_URL=http://localhost:5050
AI_API_KEY=
AI_TIMEOUT_MS=15000

MESSAGE_TTL_DAYS=14
MESSAGE_CONTEXT_LIMIT=20

# test SESSION_TTL_MINUTES=0 normally 60
# test INVITE_USED_RETENTION_HOURS=0 normally 24
